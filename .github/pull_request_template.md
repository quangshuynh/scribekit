## What changed

## Why

## Validation

<!-- Commands run and their results. -->

- [ ] `xcodebuild -project ScribeKit.xcodeproj -scheme ScribeKit -destination 'platform=macOS' build`
- [ ] `xcodebuild -project ScribeKit.xcodeproj -scheme ScribeKit -destination 'platform=macOS' test`

## Transcript and privacy impact

<!-- Does this touch raw transcripts, audio retention, stored data or the
network? State "none" if not. -->

## Energy and performance impact

<!-- New timers, polling, retained buffers or background work? State "none" if
not. -->
